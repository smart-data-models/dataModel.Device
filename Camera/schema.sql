/* (Beta) Export of data model Camera of the subject dataModel.Device for a postgreSQL database. Pending translation of enumerations and multityped attributes */
CREATE TYPE cameraType_type AS ENUM ('FIXED', 'PTZ', 'DOME', 'DAY/NIGHT', 'C-MOUNT', 'BULLET');CREATE TYPE Camera_type AS ENUM ('Camera');
CREATE TABLE Camera (address json, alternateName text, areaServed text, cameraName text, cameraNum text, cameraOrientation json, cameraType cameraType_type, cameraUsage text, dataProvider text, dateCreated timestamp, dateModified timestamp, description text, endDateTime timestamp, id text, imageSnapshot text, location json, mediaURL text, name text, on text, owner json, seeAlso json, source text, startDateTime timestamp, streamName text, streamURL text, type Camera_type);